# 待复查问题与验收清单

状态：2026-09-23 静态盘点。其中 FigureScale、RoundXYZ、DeleteNulls 已完成第一批代码加固与逻辑回归，C4D 2025.3.3 本批六项宿主测试通过（保留复杂场景与 UI 验收边界），详见 [批次记录](batch1-hardening.md)。其他项目仍是待复查项。

| 优先级 | 对象 | 证据 / 复现条件 | 下一步验收 |
| --- | --- | --- | --- |
| 高 | FigureScale | 已改为所选轴统一比例，增加无效值检查并计入世界轴长度；逻辑测试通过 | 平面、零尺寸、非单位缩放、父级缩放；拒绝无效输入且不改变对象 |
| 高 | RoundXYZ | 已初始化默认按键状态并完善撤销组结束；逻辑测试通过 | 模拟失败分支，默认按无修饰键处理；再测 Alt / Shift |
| 高 | 场景修改工具 | 多个工具没有显式 Undo；总表说明中逐项标注 | 一次撤销恢复创建、材质、颜色或变换，再重做 |
| 高 | RandomColorStandardMaterial | 直接改共享材质；非标准材质分支替换标签引用 | 两对象共享材质与非标准材质场景；先明确期望行为 |
| 中 | DeleteNulls | 已改为按前驱依次插入，并过滤重复选中层级；逻辑测试通过 | 多子级顺序、嵌套 Null、父子同时选择、Undo 后完整层级 |
| 中 | DeformerToggle | 多个所选层级合并列表未去重，修改可见性而非 Enabled | 父子同选不重复切换；核对用户期望的变形开关语义 |
| 中 | PlaneLockRatio / SquareSegementsForPlane | 有尺寸除法与持续标签驱动 | 零宽高、重复运行、保存重开、修改两轴时的优先级 |
| 中 | PositionWorldFollow | 源码为一次完整矩阵复制 | 文档已澄清；测两对象与多选顺序，不擅自改为持续跟随 |
| 中 | AnnotationFromName / TagsSameColor | 生成持续运行的标签代码 | 名称/层变化、无层、重复运行、保存重开、移除标签 |
| 中 | PresetsByHaigeC4D | 直接写多个偏好节点 | 目标版本节点存在性、原值快照和逐项恢复 |
| 低 | c4dpref.py | foo、prefs 未定义；含语言与设置草稿 | 暂保留原位并标记草稿，后续归档前检查使用关系 |

## 每个脚本的最低验收记录

记录 C4D 完整版本、测试场景、选择方式、预期与实际结果；覆盖无选择、错误类型、重复运行和恢复。持续标签工具增加保存重开和移除测试。纯 Python 静态检查不能替代 C4D 内验证。

RadialStretch 的已有宿主结果见其 README，不推广到其余脚本。
