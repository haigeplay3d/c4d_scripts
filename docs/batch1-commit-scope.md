# 第一阶段本地提交范围

## 纳入本次提交

- 根 README 和 10 个已跟踪工具的使用说明。
- docs：总表、安装、开发路线、质量清单、维护、整理记录、第一批加固记录、本清单。
- FigureScale、RoundXYZ、DeleteNulls 三个源码修复。
- tests：逻辑回归与宿主验收脚本。
- docs/validation：C4D 2025.3.3 的六项通过报告。

验证基线：14 项逻辑测试和六项固定宿主用例通过。真实窗口输入与物理 Alt/Shift 操作尚待用户反馈，不能将模拟输入等同人工交互验收。

## 保留在工作区、不纳入本次提交

| 既有变动 | 范围 | 处理 |
| --- | --- | --- |
| 已跟踪删除 | 5 个工具 tif 图标、Learn/learnc4dpython.py、7 个 pngforgithub 图片 | 保留本地删除，Git 本次不记录删除；不恢复、不清理 |
| 已跟踪修改 | TagsSameColor.py | 保留本地修改，本次不提交 |
| 未跟踪工具 | C4D_BatchDisableBasicEnabled、PresetsByHaigeC4D、RadialStretch | 整目录暂不提交，后续独立审阅；总表以无链接条目标注 |
| 本机编辑器目录 | .vs、.vscode 等 | 保持忽略，不改配置 |

Obsidian 导航属于另一个工作区，不纳入此 Git 仓库。

## 审核与交付边界

从 Git 暂存区导出本次将提交的内容，在隔离目录验证 Markdown 相对文件链接与测试，避免仅在本机可用的未跟踪文件掩盖缺失。仅提交明确文件清单，不使用 git add .。

十角色复审：产品=保留现有工具；项目=阶段提交不扩开发；技术=无重构；C4D=已有固定用例6/6，交互手测待反馈；开发=三个脚本；诊断=原有改动独立保留；QA=提交树测试及链接检查；发布=仅本地提交不推送；文档=区分10个已纳入与3个本地工具；安全=不删除、不改场景、不纳入机器专属路径。
