# 第一批脚本加固：FigureScale / RoundXYZ / DeleteNulls

日期：2026-09-23。当前状态：14 项逻辑回归通过，C4D 2025.3.3 的 r2 宿主测试 6/6 通过。以下保留首次测试与诊断过程，以末尾最终验收为准。

## 已修改

- FigureScale：按本地包围盒各轴的世界长度显示尺寸，计入已有变换；统一比例缩放，避免其他轴为零导致除零；拒绝无效目标值，保留镜像符号，新增 Undo。
- RoundXYZ：按键获取失败默认无修饰键；保留普通 / Alt / Shift 行为；异常路径也关闭撤销组。
- DeleteNulls：提升子对象时依次插入，保持顺序；父子同选只处理最上层根；层级与变换使用专用撤销类型。

## 复现与验证

修改前最初 8 个测试中 7 个失败：除零、按键变量未赋值、子级顺序颠倒、缩放计算错误、无效目标未拦截。修复后增加取消、镜像、空选择、异常结束与父子选择顺序覆盖，共 14 个测试全部通过。

```powershell
python -B -m unittest discover -s tests -v
```

测试执行真实脚本逻辑，但用最小接口替身替代不可用的 C4D 环境。Undo 的记录流程可检查，真实撤销恢复、矩阵分解及 UI 不能由这些测试证明。

## C4D 验收入口

[host_batch1.py](../tests/host_batch1.py) 在 C4D Script Manager 中打开后执行。它创建不插入当前会话文档列表的临时测试文档，不打开、保存或修改用户场景。结果打印到 Console；通过时显示 `RESULT: 6 / 6`。失败会打印 traceback，不可将仅脚本无弹窗视为通过。

覆盖 FigureScale 的平面/父级缩放、DeleteNulls 的嵌套顺序/世界矩阵、RoundXYZ 的四个输入分支，以及每项的实际 Undo/Redo。数值输入与按键状态使用固定测试输入，仍需人工检查窗口、键盘交互、父级下坐标语义与复杂剪切场景。

本轮实际尝试本机 C4D 2025 的 c4dpy，只输出许可证方式选择菜单，未执行 Python 测试。已结束这次自建探针；未修改许可、登录或全局配置。上面的宿主脚本仅通过语法检查，尚未运行。

## 文档依据

- [Maxon BaseDocument：Undo 类型与生命周期](https://developers.maxon.net/docs/py/2025_3_0/modules/c4d.documents/BaseDocument/index.html)
- [Maxon BaseObject：世界矩阵与包围盒](https://developers.maxon.net/docs/py/2025_3_0/modules/c4d/C4DAtom/GeListNode/BaseList2D/BaseObject/index.html)

## 回退和范围

本机独立备份保存三个工具修改前的完整目录，具体路径见本地维护记录。恢复时只逐个比对这三个脚本，不覆盖其他脚本或既有 Git 改动。此次未提交、未推送、未生成发布包。

## 十角色自审

1. 产品：保持三个原命令；明确 FigureScale 尺寸含义。
2. 项目：限制于第一批，不开启新脚本开发。
3. 技术：小范围修复，不引入共享库。
4. C4D：使用官方 Undo 接口，宿主验收仍有许可阻塞。
5. 开发：仅三个运行脚本和对应测试、文档。
6. 诊断：记录修复前失败与修复后结果。
7. QA：14 项逻辑回归通过；真实 Undo/Redo、矩阵、UI 未通过验收前不得宣传完成。
8. 发布：N/A，未发布；已备份原版，运行目录中的源码已更新。
9. 文档：总表、质量清单、三个 README 同步状态。
10. 安全：只结束本次创建的后台许可探针，不改变用户场景或设置。

自审结论：代码可供审阅，宿主验收仍需完成；暂不发布为已验证稳定版。


## 用户实机回传与测试 r2

用户于 2026-09-23 在 C4D 2025.3.3（2025303）回传完整日志：RESULT 1/6。DeleteNulls 的顺序、世界矩阵、Undo、Redo 通过；FigureScale 和 RoundXYZ 四种模式均在 Undo 后读值处失败，之前的操作结果断言均通过，Redo 尚未到达。

测试 r1 对前两项一直读取原 obj，对 DeleteNulls 则重新遍历文档；当前怀疑是 Undo 后旧引用导致读错对象，尚未由复测确认。r2 改为每次 Undo/Redo 后按测试对象唯一名称重新从文档取回对象，记录当前值，保留全部断言。没有修改三个正式脚本，也没有降低容差或跳过 Undo/Redo。

请在 Script Manager 重新从磁盘打开 `tests/host_batch1.py`（避免执行旧编辑缓冲区），确认控制台出现 `REVISION: batch1-r2-live-object`。结果自动保存至本机临时目录的 `c4d-scripts-host-tests/batch1-时间戳.json`，控制台打印 REPORT 路径。报告包含版本、各项状态、traceback、测试对象数值和磁盘脚本哈希，不保存用户场景。日志写入失败会明确打印 REPORT WRITE FAILED。

r2 已通过 Python 3.11 语法检查，既有 14 项逻辑测试通过；尚未实际运行 r2。当前不得宣称全部宿主验证通过。

本轮复审：产品=保留行为；项目=先定位测试差异；技术=重新获取场景对象；C4D=不推断旧引用已确认；开发=仅测试修改；诊断=5 项失败集中 Undo；QA=原断言不降级；发布=N/A 未发布；文档=本节；安全=不改场景与正式脚本。自审结论：等待 r2 实测数据。


## 最终验收：r2 通过

2026-09-23 16:02:31，用户在 C4D 2025.3.3（2025303）运行 `batch1-r2-live-object`，6/6 全部通过。

- FigureScale：平面非零轴、父级缩放、目标尺寸、实际 Undo/Redo 通过。Undo 缩放为 (2,3,4)，Redo 为 (4,6,8)。
- DeleteNulls：嵌套结构、兄弟顺序、世界矩阵、实际 Undo/Redo 通过。
- RoundXYZ：普通、Alt、Shift、输入读取失败四分支及实际 Undo/Redo 通过。位置 Undo 恢复 (1.2,2.6,-3.1)，Redo 为相应目标值。
- r1 到 r2 未改正式脚本，只在撤销/重做后重新读取场景对象：此前五项失败是测试读取旧引用造成的误报。
- [原始 JSON 报告](validation/batch1-20260923-160231.json) 已归档；报告中三个正式脚本及测试文件的磁盘 SHA256 均与当前文件匹配。

边界：这些结果验证本批六个固定用例，不等于所有复杂场景、窗口布局或真实按键交互已验收。未提交、推送或发布。

最终复审：产品=本批行为通过；项目=六项宿主闭环完成；技术=撤销后重新取当前对象；C4D=版本2025303；开发=源码与报告哈希一致；诊断=旧引用误报经复测定位；QA=14逻辑+6宿主通过，UI等边界保留；发布=N/A未发布；文档=报告和各入口已同步；安全=未操作用户场景。自审结论：本批固定测试范围验收通过。
